<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<groupId>MCNSAChallenges</groupId>

	<artifactId>MCNSAChallenges</artifactId>

	<version>1.0.1-Release</version>

	<name>mcnsaChallenges</name>

	<build>

		<resources>

			<resource>

				<directory>resources</directory>

				<filtering>true</filtering>

			</resource>

		</resources>

		<sourceDirectory>src</sourceDirectory>

		<plugins>

			<plugin>

				<artifactId>maven-compiler-plugin</artifactId>

				<version>2.3.2</version>

				<configuration>

					<source>1.7</source>

					<target>1.7</target>

				</configuration>

			</plugin>

		</plugins>

	</build>

	<dependencies>

		<dependency>

			<groupId>org.bukkit</groupId>

			<artifactId>bukkit</artifactId>

			<version>1.4.6-R0.1</version>

			<scope>system</scope>

			<systemPath>${basedir}/lib/craftbukkit-1.0.0-SNAPSHOT.jar</systemPath>

		</dependency>

		<dependency>

			<groupId>ru.tehkode</groupId>

			<artifactId>PermissionsEx</artifactId>

			<version>1.19.4</version>

			<scope>system</scope>

			<systemPath>${basedir}/lib/PermissionsEx.jar</systemPath>

		</dependency>

		<dependency>

			<groupId>com.sk89q.worldedit</groupId>

			<artifactId>WorldEdit</artifactId>

			<version>5.5.1</version>

			<scope>system</scope>

			<systemPath>${basedir}/lib/WorldEdit.jar</systemPath>

		</dependency>

		<dependency>

			<groupId>com.sk89q.worldguard</groupId>

			<artifactId>WorldGuard</artifactId>

			<version>5.7.1</version>

			<scope>system</scope>

			<systemPath>${basedir}/lib/WorldGuard.jar</systemPath>

		</dependency>

	</dependencies>

</project>